extends layouts/main.pug
include mixins/videoBlock

block content
        div.videos
            each item in videos
                +videoBlock({
                    id: item.id,
                    title: item.title,
                    views: item.views,
                    videoFile: item.fileUrl
                })